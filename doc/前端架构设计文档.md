# AI商城前端架构设计文档

## 1. 技术选型

| 技术 | 版本 | 说明 |
|------|------|------|
| Vue | 3.x | 渐进式JavaScript框架 |
| Vite | 5.x | 构建工具 |
| Vue Router | 4.x | 路由管理 |
| Pinia | 2.x | 状态管理 |
| Element Plus | 2.x | UI组件库（商家端、管理后台） |
| Vant | 4.x | 移动端UI组件库（用户端） |
| Axios | 1.x | HTTP请求库 |
| npm | 8.x+ | 包管理器 |

## 2. 项目结构

前端采用两个独立 Vue 3 项目，非 monorepo 结构。

```
华清毕设/
│
├── userfront/                         # 用户端（C端商城，Vant 4 移动端UI）
│   ├── src/
│   │   ├── api/                       # API请求封装
│   │   ├── assets/                    # 静态资源
│   │   ├── components/                # 公共组件
│   │   ├── composables/               # 组合式函数
│   │   ├── layouts/                   # 布局组件
│   │   │   └── TabBarLayout.vue       # 底部导航布局
│   │   ├── pages/                     # 页面组件
│   │   │   ├── home/                  # 首页
│   │   │   ├── category/              # 分类页
│   │   │   ├── search/                # 搜索页
│   │   │   ├── product/               # 商品详情页
│   │   │   ├── cart/                  # 购物车页
│   │   │   ├── order/                 # 订单列表、创建、详情
│   │   │   ├── user/                  # 个人中心
│   │   │   ├── login/                 # 登录页
│   │   │   ├── register/              # 注册页
│   │   │   ├── address/               # 地址管理
│   │   │   ├── favorite/              # 收藏页
│   │   │   ├── notification/          # 消息通知
│   │   │   └── merchant/              # 商家入驻、管理
│   │   ├── router/                    # 路由配置
│   │   ├── stores/                    # Pinia状态
│   │   ├── styles/                    # 样式文件
│   │   ├── utils/                     # 工具函数
│   │   ├── App.vue
│   │   └── main.js
│   ├── public/
│   ├── index.html
│   ├── vite.config.js
│   └── package.json
│
├── adminfront/                        # 管理后台（Element Plus 桌面端UI）
│   ├── src/
│   │   ├── api/                       # API请求封装
│   │   ├── assets/                    # 静态资源
│   │   ├── components/                # 公共组件
│   │   ├── composables/               # 组合式函数
│   │   ├── layouts/                   # 布局组件
│   │   │   └── DefaultLayout.vue      # 后台布局（侧边栏+顶栏）
│   │   ├── pages/                     # 页面组件
│   │   │   ├── login/                 # 登录页
│   │   │   ├── dashboard/             # 数据概览
│   │   │   ├── user/                  # 用户管理
│   │   │   ├── merchant/              # 商家管理
│   │   │   ├── product/               # 商品管理
│   │   │   ├── category/              # 分类管理
│   │   │   ├── attribute/             # 属性管理
│   │   │   ├── order/                 # 订单管理
│   │   │   ├── banner/                # 轮播图管理
│   │   │   ├── config/                # 系统配置
│   │   │   ├── file/                  # 文件管理
│   │   │   ├── notification/          # 消息通知
│   │   │   └── log/                   # 操作日志
│   │   ├── router/                    # 路由配置
│   │   ├── stores/                    # Pinia状态
│   │   ├── styles/                    # 样式文件
│   │   ├── utils/                     # 工具函数
│   │   ├── App.vue
│   │   └── main.js
│   ├── vite.config.js
│   └── package.json
│
└── backend/                           # 后端项目（独立目录）
```

## 3. 各端路由配置

### 3.1 用户端路由（userfront，Vant UI）

| 路由 | 页面 | 说明 |
|------|------|------|
| / | 首页 | 轮播图、推荐商品、分类导航 |
| /category | 分类页 | 商品分类列表 |
| /search | 搜索页 | 商品搜索 |
| /product/:id | 商品详情 | 商品信息、SKU选择 |
| /cart | 购物车 | 购物车列表 |
| /order | 订单列表 | 我的订单 |
| /order/create | 创建订单 | 确认订单、选择地址 |
| /order/:id | 订单详情 | 订单信息、物流 |
| /user | 个人中心 | 用户信息、功能入口 |
| /address | 地址管理 | 收货地址 |
| /favorite | 我的收藏 | 收藏商品 |
| /notification | 消息通知 | 站内信列表 |
| /login | 登录 | 用户登录 |
| /register | 注册 | 用户注册 |
| /merchant/apply | 商家入驻 | 商家入驻申请 |
| /merchant | 商家信息 | 商家店铺信息 |
| /merchant/products | 商品管理 | 商家商品列表 |
| /merchant/product/edit | 编辑商品 | 商家编辑商品 |

### 3.2 管理后台路由（adminfront，Element Plus）

| 路由 | 页面 | 说明 |
|------|------|------|
| /login | 登录 | 管理员登录 |
| /dashboard | 数据概览 | 平台数据统计 |
| /user | 用户管理 | 用户列表 |
| /merchant | 商家管理 | 商家列表 |
| /product | 商品管理 | 商品列表 |
| /category | 分类管理 | 分类列表 |
| /attribute | 属性管理 | 属性模板 |
| /order | 订单管理 | 订单列表 |
| /banner | 轮播图管理 | 轮播图列表 |
| /notification | 消息通知 | 消息管理 |
| /config | 系统配置 | 配置项管理 |
| /file | 文件管理 | 文件列表 |
| /log | 操作日志 | 操作记录 |

## 4. 工具函数设计

两个项目各自维护独立的工具函数，代码结构相似但不共享包。

### 4.1 工具函数

```javascript
// src/utils/format.js
export const formatPrice = (price) => Number(price).toFixed(2)
export const formatPhone = (phone) => phone?.replace(/(\d{3})\d{4}(\d{4})/, '$1****$2')
export const formatDate = (date, fmt = 'YYYY-MM-DD HH:mm:ss') => { /* ... */ }
```

```javascript
// src/utils/storage.js
const PREFIX = 'ai_mall_'
export const getStorage = (key) => JSON.parse(localStorage.getItem(PREFIX + key))
export const setStorage = (key, val) => localStorage.setItem(PREFIX + key, JSON.stringify(val))
export const removeStorage = (key) => localStorage.removeItem(PREFIX + key)
```

### 4.2 API封装

```javascript
// src/api/request.js
import axios from 'axios'
import { getToken } from '../utils/storage'

const request = axios.create({
  baseURL: import.meta.env.VITE_API_BASE_URL || '/api',
  timeout: 15000
})

request.interceptors.request.use(config => {
  const token = getToken()
  if (token) config.headers.Authorization = `Bearer ${token}`
  return config
})

request.interceptors.response.use(
  res => {
    const { code, message, data } = res.data
    if (code === 200) return data
    return Promise.reject(new Error(message))
  },
  error => {
    if (error.response?.status === 401) {
      window.location.href = '/login'
    }
    return Promise.reject(error)
  }
)

export default request
```

## 5. 构建与运行

```bash
# 用户端
cd userfront && npm install && npm run dev

# 管理后台
cd adminfront && npm install && npm run dev
```

## 6. 部署方式

```
user.example.com   → 用户端（userfront）
admin.example.com  → 管理后台（adminfront）
```