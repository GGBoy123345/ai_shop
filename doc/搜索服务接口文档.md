# 搜索服务接口文档

> **注意：** 当前搜索使用 MySQL LIKE 查询实现，未集成 Elasticsearch。

> **服务名称**：搜索服务（search-service）
> **基础路径**：`/api`（外部接口）、`/internal`（内部接口）
> **协议**：HTTP/HTTPS
> **数据格式**：JSON

---

## 统一响应格式

所有接口均返回以下统一格式：

```json
{
  "code": 200,
  "message": "success",
  "data": {}
}
```

| 字段 | 类型 | 说明 |
|------|------|------|
| code | Integer | 业务状态码，200 表示成功，其他为错误码 |
| message | String | 提示信息 |
| data | Object | 业务数据，具体结构见各接口说明 |

---

# 第一部分：外部接口（/api/**，前端调用）

---

## 一、搜索模块

---

### 1. 商品搜索

根据关键词、分类、价格区间等条件搜索商品，支持多种排序方式和分页。基于 MySQL LIKE 查询实现。

**请求方式与URL**

`GET /api/search`

**请求头**

| 参数名 | 是否必填 | 说明 |
|--------|----------|------|
| Content-Type | 否 | application/json |
| Authorization | 否 | Bearer {token}，登录后可获取个性化搜索结果 |

**请求参数（Query）**

| 参数名 | 类型 | 是否必填 | 说明 |
|--------|------|----------|------|
| keyword | String | 是 | 搜索关键词，支持商品名称、副标题模糊匹配 |
| categoryId | Long | 否 | 分类ID，筛选指定分类下的商品 |
| minPrice | BigDecimal | 否 | 最低价格，大于等于0 |
| maxPrice | BigDecimal | 否 | 最高价格，大于等于0，需大于等于minPrice |
| sort | String | 否 | 排序方式：`default`（默认综合排序）、`price_asc`（价格升序）、`price_desc`（价格降序）、`sales_desc`（销量降序）、`newest`（最新上架）。不传则默认综合排序 |
| page | Integer | 否 | 页码，默认1 |
| size | Integer | 否 | 每页数量，默认10，最大50 |

**请求示例**

```
GET /api/search?keyword=连衣裙&categoryId=5&minPrice=100&maxPrice=500&sort=sales_desc&page=1&size=10
```

**响应示例**

```json
{
  "code": 200,
  "message": "success",
  "data": {
    "total": 128,
    "page": 1,
    "size": 10,
    "keyword": "连衣裙",
    "list": [
      {
        "id": 1001,
        "name": "2024新款连衣裙 夏季清凉修身",
        "subtitle": "优质面料，舒适透气",
        "mainImage": "https://cdn.example.com/products/1001/main.jpg",
        "price": 199.00,
        "marketPrice": 399.00,
        "sales": 500,
        "shopName": "时尚女装店",
        "categoryId": 5,
        "categoryName": "连衣裙",
        "score": 12.5
      },
      {
        "id": 1002,
        "name": "法式碎花连衣裙 温柔气质",
        "subtitle": "法式浪漫，优雅出行",
        "mainImage": "https://cdn.example.com/products/1002/main.jpg",
        "price": 259.00,
        "marketPrice": 499.00,
        "sales": 320,
        "shopName": "优选服饰专营",
        "categoryId": 5,
        "categoryName": "连衣裙",
        "score": 10.8
      }
    ]
  }
}
```

**响应字段说明**

| 字段 | 类型 | 说明 |
|------|------|------|
| total | Long | 符合条件的商品总数 |
| page | Integer | 当前页码 |
| size | Integer | 每页数量 |
| keyword | String | 当前搜索关键词 |
| list | Array | 商品列表 |
| list[].id | Long | 商品ID |
| list[].name | String | 商品名称（搜索关键词高亮展示） |
| list[].subtitle | String | 商品副标题 |
| list[].mainImage | String | 商品主图URL |
| list[].price | BigDecimal | 售价 |
| list[].marketPrice | BigDecimal | 市场价（划线价） |
| list[].sales | Integer | 累计销量 |
| list[].shopName | String | 店铺名称 |
| list[].categoryId | Long | 分类ID |
| list[].categoryName | String | 分类名称 |
| list[].score | BigDecimal | 搜索相关度评分，仅排序方式为default时有参考意义 |

**业务说明**

- 搜索结果基于 MySQL LIKE 查询实现
- 综合排序（default）考虑销量、商品权重等因素
- 价格区间筛选为闭区间 [minPrice, maxPrice]
- 未登录用户搜索结果与已登录用户相同，后续可扩展个性化推荐

**错误码**

| 错误码 | 说明 |
|--------|------|
| 40001 | 参数错误（keyword为空、价格范围不合法、排序方式无效、分页参数不合法等） |

---

### 2. 热门搜索关键词

获取当前平台的热门搜索关键词列表，返回前10个热门搜索词。用于搜索页面的热门搜索展示。

**请求方式与URL**

`GET /api/search/hot`

**请求头**

| 参数名 | 是否必填 | 说明 |
|--------|----------|------|
| Content-Type | 否 | application/json |

**请求参数**

无

**响应示例**

```json
{
  "code": 200,
  "message": "success",
  "data": [
    {
      "keyword": "连衣裙",
      "score": 9856
    },
    {
      "keyword": "T恤",
      "score": 8720
    },
    {
      "keyword": "运动鞋",
      "score": 7500
    },
    {
      "keyword": "手机壳",
      "score": 6890
    },
    {
      "keyword": "蓝牙耳机",
      "score": 6200
    },
    {
      "keyword": "牛仔裤",
      "score": 5800
    },
    {
      "keyword": "防晒霜",
      "score": 5100
    },
    {
      "keyword": "背包",
      "score": 4700
    },
    {
      "keyword": "短裤",
      "score": 4200
    },
    {
      "keyword": "拖鞋",
      "score": 3900
    }
  ]
}
```

**响应字段说明**

| 字段 | 类型 | 说明 |
|------|------|------|
| keyword | String | 热门搜索关键词 |
| score | Integer | 热度分数，基于搜索频次计算，值越大越热门 |

**业务说明**

- 热门搜索词基于用户搜索行为统计，定时更新（建议每小时刷新一次）
- 返回结果按热度分数降序排列，固定返回前10条
- 此接口无需登录，数据为平台级公共数据

**错误码**

| 错误码 | 说明 |
|--------|------|
| 无 | 该接口不返回业务错误，数据为空时返回空数组 |

---

### 3. 搜索建议

用户在搜索框输入关键词时，实时返回搜索建议列表（自动补全）。基于商品标题进行前缀匹配和分词匹配，返回最多10条建议。

**请求方式与URL**

`GET /api/search/suggest`

**请求头**

| 参数名 | 是否必填 | 说明 |
|--------|----------|------|
| Content-Type | 否 | application/json |

**请求参数（Query）**

| 参数名 | 类型 | 是否必填 | 说明 |
|--------|------|----------|------|
| keyword | String | 是 | 用户输入的搜索关键词（前缀），最少1个字符 |

**请求示例**

```
GET /api/search/suggest?keyword=连衣
```

**响应示例**

```json
{
  "code": 200,
  "message": "success",
  "data": [
    "连衣裙",
    "连衣裙女夏",
    "连衣裙长款",
    "连衣裙修身显瘦",
    "连衣裙碎花"
  ]
}
```

**响应字段说明**

| 字段 | 类型 | 说明 |
|------|------|------|
| data | Array[String] | 搜索建议列表，为商品标题字符串数组，按搜索热度和匹配度排序 |

**业务说明**

- 建议结果基于搜索历史+商品标题混合策略
- keyword 长度为1-50个字符，超出截断
- 返回结果去重，最多10条
- 前端应在用户输入时发起请求，建议增加防抖（debounce）处理，避免过于频繁调用

**错误码**

| 错误码 | 说明 |
|--------|------|
| 40001 | 参数错误（keyword为空） |

---

### 4. 搜索历史

获取当前登录用户最近的搜索历史记录，返回最多20条。搜索历史按时间倒序排列，最新搜索的排在最前面。

**请求方式与URL**

`GET /api/search/history`

**请求头**

| 参数名 | 是否必填 | 说明 |
|--------|----------|------|
| Content-Type | 否 | application/json |
| Authorization | 是 | Bearer {token} |

**请求参数**

无

**响应示例**

```json
{
  "code": 200,
  "message": "success",
  "data": [
    {
      "id": 101,
      "keyword": "连衣裙",
      "createTime": "2026-05-28 15:30:00"
    },
    {
      "id": 100,
      "keyword": "运动鞋",
      "createTime": "2026-05-28 14:20:00"
    },
    {
      "id": 99,
      "keyword": "蓝牙耳机",
      "createTime": "2026-05-27 10:15:00"
    },
    {
      "id": 98,
      "keyword": "手机壳",
      "createTime": "2026-05-26 09:00:00"
    }
  ]
}
```

**响应字段说明**

| 字段 | 类型 | 说明 |
|------|------|------|
| id | Long | 搜索历史记录ID |
| keyword | String | 搜索关键词 |
| createTime | String | 搜索时间 |

**业务说明**

- 仅登录用户可查看搜索历史
- 搜索历史在用户每次调用商品搜索接口（GET /api/search）时自动记录
- 同一用户搜索相同关键词时，更新该记录的时间（不重复存储）
- 最多保留最近20条记录，超出后自动删除最早的记录

**错误码**

| 错误码 | 说明 |
|--------|------|
| 40101 | 未登录或Token已过期 |

---

### 5. 清空搜索历史

清空当前登录用户的全部搜索历史记录。操作不可恢复。

**请求方式与URL**

`DELETE /api/search/history`

**请求头**

| 参数名 | 是否必填 | 说明 |
|--------|----------|------|
| Authorization | 是 | Bearer {token} |

**请求参数**

无

**响应示例**

```json
{
  "code": 200,
  "message": "搜索历史已清空",
  "data": null
}
```

**业务说明**

- 仅登录用户可操作
- 清空后不可恢复
- 仅删除当前用户的搜索历史，不影响其他用户

**错误码**

| 错误码 | 说明 |
|--------|------|
| 40101 | 未登录或Token已过期 |

---

# 第二部分：内部接口

> 当前搜索服务无内部接口。搜索基于 MySQL LIKE 查询实现，无需 ES 索引同步。

---

# 附录

## 附录A：错误码汇总

### 通用错误码

| 错误码 | 说明 |
|--------|------|
| 200 | 成功 |
| 400 | 请求参数错误 |
| 401 | 未授权（未登录） |
| 403 | 禁止访问（权限不足） |
| 404 | 资源不存在 |
| 500 | 服务器内部错误 |

### 业务错误码 - 通用（40001）

| 错误码 | 说明 |
|--------|------|
| 40001 | 参数错误 |

### 权限与认证错误码

| 错误码 | 说明 |
|--------|------|
| 40101 | 未登录或Token已过期 |

---

## 附录B：接口总览

### 外部接口（/api/**）

| 序号 | 接口名称 | 请求方式 | URL | 是否需登录 |
|------|----------|----------|-----|-----------|
| 1 | 商品搜索 | GET | /api/search | 否 |
| 2 | 热门搜索关键词 | GET | /api/search/hot | 否 |
| 3 | 搜索建议 | GET | /api/search/suggest | 否 |
| 4 | 搜索历史 | GET | /api/search/history | 是 |
| 5 | 清空搜索历史 | DELETE | /api/search/history | 是 |
